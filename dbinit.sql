CREATE TABLE settings (id INTEGER PRIMARY KEY AUTOINCREMENT, active BOOLEAN);
CREATE TABLE votes (id INTEGER PRIMARY KEY AUTOINCREMENT, direction INTEGER, fromNumber VARCHAR, created_at DATETIME, active BOOLEAN);

INSERT INTO settings (active) VALUES (1);